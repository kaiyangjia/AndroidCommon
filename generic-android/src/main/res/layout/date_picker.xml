<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:divider="@drawable/divider_trans"
    android:orientation="horizontal"
    android:padding="5dp"
    android:showDividers="middle">

    <TextView
        android:id="@+id/date_picker_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_gravity="center_vertical"
        android:text="开始日期" />

    <Button
        android:id="@+id/date_picker_btn"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_width="20dp"
        android:layout_height="23dp"
        android:layout_marginLeft="4dp"
        android:background="@drawable/selector_icon_calender" />

    <TextView
        android:id="@+id/date_picker_string"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="4dp"
        android:layout_marginRight="4dp"
        android:gravity="center"
        android:layout_toRightOf="@id/date_picker_title"
        android:layout_toLeftOf="@id/date_picker_btn"
        android:padding="5dp"
        android:text="          "/>
</RelativeLayout>